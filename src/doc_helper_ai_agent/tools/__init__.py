"""Agent tools.

Each tool wraps a service/infrastructure call and returns a uniform
:class:`~doc_helper_ai_agent.schemas.tools.ActionResult` so the agent can record
a consistent, serialisable audit trail of actions taken.
"""
